{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "servicePrincipalAppId": {
            "reference": {
                "keyVault": {
                    "id": "MISSING"
                },
                "secretName": "env-aks-app-id"
            }
        },
        "servicePrincipalObjectId": {
            "reference": {
                "keyVault": {
                    "id": "MISSING"
                },
                "secretName": "env-aks-object-id"
            }
        },
        "servicePrincipalSecret": {
            "reference": {
                "keyVault": {
                    "id": "MISSING"
                },
                "secretName": "env-aks-password"
            }
        },
        "sshPublicKey": {
            "reference": {
                "keyVault": {
                    "id": "MISSING"
                },
                "secretName": "env-aks-ssh-public-key"
            }
        },
        "dnsPrefix": {
            "reference": {
                "keyVault": {
                    "id": "MISSING"
                },
                "secretName": "env-aks-dns-prefix"
            }
        }
    }
}